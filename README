XOR-связный список — структура данных, похожая на обычный двусвязный список, однако в каждом элементе хранится только один составной адрес — результат выполнения операции XOR над адресами предыдущего и следующего элементов списка.
(см. https://ru.wikipedia.org/wiki/XOR-связный_список)

Задание: 
1. Дописать реализацию всех предложенных методов данного класса в файлах xor_list.cpp и xor_list.hpp (можно вносить изменения в сигнатуры методов, но все они должны присутствовать).
2. Написать в файле test.cpp код для тестирования класса.
3. Дописать в Makefile цели для сборки, запуска теста, очистки всех лишних файлов.
4. Открыть в данном репозитории Pull Request со своим кодом (желательно, чтобы вас можно было идентифицировать).
